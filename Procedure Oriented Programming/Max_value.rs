fn main() {
    // Variables
    let x = 5;
    let y = 10;
    let sum = x + y;

    println!("Sum of {} and {} is: {}", x, y, sum);

    // Constants
    const MAX_VALUE: i32 = 100;
    println!("Maximum value: {}", MAX_VALUE);
}
